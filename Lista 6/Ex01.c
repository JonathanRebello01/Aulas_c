// Desenvolva um algoritmo que efetue a leitura dos elementos inteiros de um vetor A com 5
// posições e que esses valores sejam passados para um vetor B, verificando que, se o índice do
// vetor for um número par, o elemento a ser passado deverá ser multiplicado por 5 e, se for
// ímpar, deverá ser somando de 5.

#include<stdio.h>
#define AUX 0



int main(){

    int A[5], B[5];
    int i;

    for (i=0;i<=4;i++){
        printf("%d", i);

    }   

    return 0;
}